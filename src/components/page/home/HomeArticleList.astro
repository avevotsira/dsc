---
import { getArticleUrl } from "@/lib/route";

import HomeArticleCard from "./HomeArticleCard.astro";
import type { SupportedLanguage } from "@/i18n/ui";
import type { CollectionEntry } from "astro:content";

interface Props {
  articles: CollectionEntry<"articles">[];
  lang: SupportedLanguage;
}

const { articles, lang } = Astro.props;
---

<ul
  class="grid list-none grid-cols-1 gap-5 p-5 md:grid-cols-2 lg:grid-cols-3"
  role="list"
>
  {
    articles.map((article) => (
      <li>
        <HomeArticleCard
          image={article.data.image}
          title={article.data.title}
          body={article.body}
          href={getArticleUrl(article.slug, lang)}
          date={article.data.date}
          lang={lang}
        />
      </li>
    ))
  }
</ul>
